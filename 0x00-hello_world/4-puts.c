#include <stdio.h>
/**
 *main - main entry point
 *return : always 0 (succes)
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	/*the main will return the value 0*/
	return (0);
}
